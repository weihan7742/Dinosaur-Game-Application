package game;

/**
 * An interface to allow classes to implement it to allow interaction
 * of different game features with currency of the game.
 */
public interface EcoPointInterface {
    EcoPoint ecoPoint = new EcoPoint();
}
